# OMPC Physiotherapy — Website

This workspace contains a redesigned `index.html` (immediate fixes applied), plus `sitemap.xml` and `robots.txt` scaffolding.

Next recommended steps:
- Replace the GA4 placeholder ID (`G-XXXXXXXXXX`) in the head of `index.html` with your real GA4 measurement ID.
- Replace the YouTube embed `src` with the clinic's actual video ID or swap the iframe for a hosted video.
- Create dedicated service pages under `/services/` with 300+ words per page (orthopedic, neurological, sports & pediatric).
- Claim and optimize Google Business Profile; add responsive Google Map embed to the contact section.
- Create metadata files and content for doctor profile, testimonials, and FAQ to improve E-E-A-T.
